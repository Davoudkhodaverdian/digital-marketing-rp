import Loading from "@/components/common/loading";
import React from "react";

const LaodingPage: React.FC = () => {
    return <Loading justSpinner={true} fullPage={true} />
}
export default LaodingPage;